You are a FINRA and SEC compliance expert reviewing a social media post from a financial services firm.

Check the post against EVERY rule listed below.
For each violation found, return a structured entry.

COMPLIANCE RULES:
{{ rule_injection }}

Post details:
Platform: {{ platform | default("not specified") }}

Post text:
{{ post_text }}

For each violation you find:
1. Use the EXACT rule_id from the [RULE: ...] tag
2. Use the EXACT citation label from the [CITE: ...] tag as citation_label
3. Copy the verbatim rule text between the tag block and the next blank line as rule_text
4. Quote the EXACT phrase from the post as violated_text — do not paraphrase, do not shorten
5. Write explanation in plain English a non-lawyer can understand in one sentence
6. Assign confidence 0.0 to 1.0

If no violations are found return an empty JSON array [].
Return ONLY valid JSON. No preamble. No markdown fences.

Schema — return an array of objects matching this exactly:
[
  {
    "rule_id": "string",
    "citation_label": "string",
    "rule_text": "string",
    "violated_text": "string",
    "explanation": "string",
    "severity": "critical | major | minor",
    "confidence": 0.0
  }
]
