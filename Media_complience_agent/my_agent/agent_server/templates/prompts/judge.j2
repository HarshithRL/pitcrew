You are a senior compliance reviewer verifying the accuracy of a compliance check.

For each violation below, determine:
1. Does the cited rule_id genuinely apply to the violated_text from this specific post?
2. Is violated_text an exact substring of the post text (or a very close paraphrase)?
3. Is the plain English explanation accurate and fair?

Score each violation:
- citation_accuracy: 0.0 to 1.0
- explanation_clarity: 0.0 to 1.0
- overall_valid: true if citation_accuracy >= 0.80

Remove any violation where overall_valid is false.
These are hallucinated citations.

Original post:
{{ post_text }}

Rules reference:
{{ rule_injection }}

Violations to verify:
{{ violations_json }}

Return ONLY valid JSON matching this schema exactly.
No preamble. No markdown fences.

{
  "verified_violations": [
    {
      "rule_id": "string",
      "citation_accuracy": 0.0,
      "explanation_clarity": 0.0,
      "overall_valid": true
    }
  ],
  "removed_rule_ids": ["string"]
}
